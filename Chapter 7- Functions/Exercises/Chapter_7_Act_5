#Calculating the area of a square and checking if it is a square or not. 

length = int(input())
width = int(input())
def area(length, width):
	print (length * width )
	
	if length == width:
          print ("Square")
	else: 
		print("Not square")

area(length,width)